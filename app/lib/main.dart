import 'package:flutter/material.dart';

void main() {
  runApp(const SharlyLight());
}

class SharlyLight extends StatelessWidget {
  const SharlyLight({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
